<?php 

    #Condicinales usando los operadores Ternarios -----------

    $a=10;
    $b=10;

    $valor;

    $valor = $a>$b ? 'Mayor' : ($a < $b ? 'Menor' : 'Igual');

    echo "el valor de a es $valor que el valor de b";
